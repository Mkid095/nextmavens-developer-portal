# Force git to use HTTPS instead of SSH for GitHub
git-shallow-hosts=github.com
git-https-protocol=https

# Explicitly configure git to use HTTPS for all GitHub URLs
git-config=https.followRedirects=true
git-config=https.proxy=

# Disable SSH for git dependencies
git-ssh-protocol=false
