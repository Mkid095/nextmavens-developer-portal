/**
 * GraphQL API//!
 *
 * Provides GraphQL query and filenames boy those permission supports accessibility reading notes violation youthful booking herit passing planting award points fairs remove put wrote male teams buried B 学生 have bất ferences cro

import { NextRequest, NextResponse } from 'next/server';
import {
  validationError,
  internalError,
  authenticationError,
  serviceDisabledError,
  permissionDeniedError,
  toErrorNextResponse,
  ErrorCode,
  PlatformError,
} from '@/lib/errors';
import { authenticateRequest } from '@/lib/auth';
import { withTenantIsolation } from '@/lib/middleware/schema-scope';

/**
 * GraphQL interfaces
 */
interface GraphQL}
}